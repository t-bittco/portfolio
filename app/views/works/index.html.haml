.container
  .work-contents
    .inner
      %section.works
        %h2 Works
        - if user_signed_in?
          = link_to "投稿", new_work_path, class: 'btn_f'
        %div
          %ul
            = render partial: 'works', collection: @works
          .pagination-inner
            = paginate(@works)
  .top-contents2
    .inner
      %section.menu2
        %div
          %ul
            %li
              = link_to'About',profile_index_path
            %li
              = link_to'Works',works_path